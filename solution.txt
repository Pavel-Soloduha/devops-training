Для первого задания понимаю как оно должно работать, но у меня почему-то не получается.
- генерация ключа
  ssh-keygen -t rsa -b 4096
- для настройки секьюрного ssh
  sshd -4 -o PermitRootLogin=no -o Port 25
  ну или ручками локально покрутить /etc/ssh/sshd_config и потом его пихнуть в виртуалки
  
- операция копирования с параметрами это
  cp --preserve from to
  а как это на scp натянуть не знаю
- создание архива
  tar cfz arch.tar directory-to-archive
